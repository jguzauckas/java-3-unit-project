/**
 * Write a description of class PriceCompare here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class PriceCompare
{
    /**
     * This method takes a price of something at the store and compares it to the
     * manufacturer's suggested retail price (MSRP).
     * 
     * For this method, a price is reasonable if it is less than twice the MSRP
     *
     * @param  price  the price of something
     *         msrp   the MSRP of the same thing
     * @return    whether or not the price is reasonable
     * 
     * 5 points
     */
    public static boolean reasonable(double price, double msrp)
    {
        //Missing code segment
    }
    
    /**
     * This method takes a price of something at a store and compares it to all
     * time low and high prices to determine if you should buy it.
     * 
     * When the price is below the all time low, the method should print
     * "Buy right now!"
     * 
     * When the price is above the all time high, the method should print
     * "Do not buy!"
     * 
     * When the price is greater than low but less than the halfway point between
     * low and high, the method should print "Not a bad price!"
     * 
     * When the price is less than high but greater than the halfway point between
     * low and high, the method should print "A little too expensive!"
     * 
     * 5 points
     */
    public static void checkAllTime(double price, double low, double high)
    {
        //Missing code segment
    }
}
